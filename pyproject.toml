[project]
name = "f1_predictor"
version = "0.1.0"
requires-python = ">=3.10"

# MLflow lives here because both training and serving touch it
dependencies = [
    "onnx",
    "pydantic",
    "pydantic-settings",
    "mlflow>=2.0",
    "pandera[pandas]"
]

[tool.setuptools.packages.find]
where = ["src", "."]
include = ["f1_predictor*", "ingest*"]

[project.optional-dependencies]
data = [
    "lakefs"
]
train = [
    "lightgbm",
    "pandas",
    "pandera",
    "scikit-learn",
    "notebook",
    "onnxmltools",
]
serv = [
    "fastapi",
    "uvicorn",
    "onnxruntime",
    "numpy",
    "jinja2>=3.1.0",
]
dev = [
    "pytest",
    "ruff",
]

[project.scripts]
f1_serve = "f1_predictor.serve.api:main"
f1_bootstrap = "ingest.bootstrap:run"
f1_eval = "f1_predictor.pipelines.eval_pipeline:eval_pipeline"
f1_train = "f1_predictor.pipelines.train_pipeline:train_pipeline"